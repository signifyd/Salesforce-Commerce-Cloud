'use strict';

var StringUtils = {
    encodeBase64: function (value) {
        return value + 'encoded64';
    },
    formatCalendar: function () {
        return 'formatted calendar';
    }
};

module.exports = StringUtils;
